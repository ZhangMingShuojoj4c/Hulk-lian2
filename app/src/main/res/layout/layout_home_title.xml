<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <ImageView
        android:layout_width="@dimen/home_img_width"
        android:layout_height="@dimen/home_img_heigth"
        app:srcCompat="@drawable/ic_home"
        android:id="@+id/F_HOME_img" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@+id/F_HOME_img"
        android:layout_toEndOf="@+id/F_HOME_img"
        android:layout_alignBottom="@+id/F_HOME_img"
        android:text="My Home"
        android:textSize="@dimen/home_edt_text_size"
        android:id="@+id/F_HOME_edtNameHome"/>

</RelativeLayout>